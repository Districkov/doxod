# Копируй в .env и заполни значениями
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# NextAuth - нужен хотя бы один из этих:
AUTH_SECRET="генерируй: openssl rand -base64 32"
# или
NEXTAUTH_SECRET="генерируй: openssl rand -base64 32"

# Опционально (Vercel автоматически устанавливает)
NEXTAUTH_URL="https://твой-домен.vercel.app"
